import type { Options } from 'prettier';

const config: Options = {
  semi: true,
  tabWidth: 2,
  printWidth: 120,
  singleQuote: true,
  trailingComma: 'es5',
  endOfLine: 'lf',
};

export default config;
